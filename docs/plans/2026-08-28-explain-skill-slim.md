# explain skill 瘦身 + 定分层硬判据

> Opened: 2026-08-28 · commit 4229fe1
> 来源：原 backlog T-13

## 目标

两个问题一起解。

**问题一 · 重叠已经变成漂移。** `owncc.md` 已收了画图规范、颜文字、分层结构、追问菜单格式，`SKILL.md` 里还留着同样内容，且**已经对不上了**：

| 项 | `SKILL.md` 现值 | `owncc.md` / 原 D-07 定论 |
|---|---|---|
| 图宽上限 | 60 列 | 87 列 |
| 对齐办法 | 逐字符核对中文占 2 列 | 框内只放 ASCII，中文贴框外 |

「逐字符核对」是原 D-07 明确判死的规则——它要求模型做机械计数，而这正是模型不可靠之处。这条死规则此刻还活在 skill 里。

**问题二 · 分层交付没生效。** owncc 首测暴露：规则说「先给最浅的一层」，但「最浅」没有可操作判据，模型无从判断，结果一口气讲完。

## 边界

- **不动** `description` 的触发条件部分，除非写 code-walkthrough 时发现边界冲突（那属于下一个任务）
- **不往 `owncc.md` 里加**分层判据——它只在讲解时适用，不是无条件规范

## 推断的前提（待确认）

- 假设「首轮输出最多两个 `▍` 标题，给完追问菜单必须停」是可用的硬判据——它可检验、不含糊，但**未实测**是否真能拦住一口气讲完
- 假设 skill 单独使用（用户没启用 owncc）时格式靠模型自由发挥是可接受的降级——这是切法 A 的既定约定

## 完成判据

- [x] `SKILL.md` 中「画图」整节（判据 / 规格 / 节点数处置）删除，`owncc.md` 里已有的不再出现第二份
- [x] 「标记」一节删除（颜文字与标记归 `owncc.md`）
- [x] 分层硬判据写入，形式为可检验的约束而非形容词
- [ ] 实测：问一个五层深度的概念，首轮输出停在追问菜单，未一次讲完 — **阻塞，见下**
- [x] 实测：`SKILL.md` 全文搜「60 列」「逐字符」零匹配

## 进度

- 2026-08-28 从 backlog 拆出，未开始

## 实际改动

123 → 86 行。除 plan 预定的两节外，另删两处与 `owncc.md` 正面冲突的内容：

| 冲突项 | 原 `SKILL.md` | `owncc.md` 定的 |
|---|---|---|
| 追问菜单格式 | 自定 `**往下看哪个**：1. 2. 3.` | 引用块里的 `▍延伸方向` |
| 视觉标记 | ⚠️ 💡 ❌ 三个 emoji | 已放弃 emoji，改用 `▍★ Insight` + 颜文字 |

emoji 那套是 T-02 早已推翻的方案，与「60 列」「逐字符核对」属同类残留。

**硬判据没按原建议写。** 原建议是「首轮最多两个 `▍` 标题」，实际改为「五步一步都不许碰 + 250 字 + 停在菜单」——标题数量与讲多深没有必然关系，而「把五步一次讲完」才是要防的失败模式，判据应当直接钉在它上面。

## 遗留

**实测无法进行**：`.claude/skills/` 已在上一个任务中删除，插件也尚未打包，当前没有任何路径能加载这个 skill。验证方式待定，见 `context.md` 结构性坑第三条。

## 进度

- 2026-08-28 从 backlog 拆出
- 2026-09-06 瘦身完成，静态判据全过；行为实测阻塞于加载路径
