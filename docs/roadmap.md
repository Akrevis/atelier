# 演进路线

> 项目怎么走到今天：分阶段，每阶段列出结论、对应的决策记录和落地提交。
> **只存指针，不存状态。** 当前在做什么看 `context.md` 的活跃任务表。

---

## 阶段一 · 设计定型（2026-08-24 ~ 2026-08-28）

**这一批解决的问题**：定位、产物划分、以及所有会阻塞动手的设计缺口。结论全部落在 `design.md`，理由在 `devlog.md`。

| 结论 | 决策记录 | 落地提交 |
|---|---|---|
| 定位从「治理解的错觉」改为「让输出可学」——AI 负责讲清楚，用户的理解能力不是 AI 要管的变量 | devlog「定位从…改成…」 | `32ddd12` |
| 沉浸模式整合进同一个包，不独立成包；代价是靠「不激活时完全静默」缓解 | devlog「否决：沉浸模式独立成包」 | `32ddd12` |
| 卡壳解锁从三件套改为三档，依据样例效应与专长逆转效应 | devlog「否决：卡壳解锁三件套」 | `32ddd12` |
| 复习调度用 SM-2 式反馈控制，不拟合遗忘曲线 | devlog「否决：绘制个人遗忘曲线」 | `32ddd12` |
| MVP 划线：日常模式两个 skill + `/palette`，沉浸模式留第二版 | 原 T-01 | `7834f34` |
| `references/` 只能放按需查阅的材料，每次都要遵守的规则必须进 `SKILL.md` 正文 | 原 D-06 | `7834f34` |
| ASCII 形态选闭合框（实测推翻了原推断）；「逐字符核对宽度」判死，改为框内只放 ASCII、中文贴框外；宽度上限 87 列 | devlog「实测：三种画法对比」+ 原 D-07 | `9467d57` `7a6a784` |
| `/palette` 定为临时白板，固定 `~/.claude/atelier/palette.html`，取消全局/项目区分 | 原 D-02 | `a5f2b9c` |
| 持久化知识地图分离为独立需求，不与临时画板混在一个机制里 | 原 T-12 | `9467d57` |
| 复述验收触发时机定死：两个条件同时满足才要，一次会话最多一次 | 原 D-04 | `b91226b` |
| 9~15 节点的图拆成两张分层图或只画主干，不掉进 ASCII 与 `/palette` 之间的缝里 | 原 D-03 | `b91226b` |
| 视觉标记放弃 emoji，改用 `▍★ Insight` 块级标记 + 行内代码包裹的颜文字 | 原 T-02 | `011f21a` |
| output-styles 从否决改为采用，按维度切（切法 A）：`owncc.md` 管怎么说，插件管做什么 | devlog「推翻：output-styles 从否决改为采用」 | `011f21a` |
| 插件能分发 output style，但仍不装——独立性、style 位、`force-for-plugin` 静默覆盖 | devlog「插件能装 output-styles，但仍不装」 | `4229fe1` |
| CLAUDE.md 清理掉与 `owncc.md` 重复的内容，判据是「切走 style 后这条失效会不会出事」 | devlog「CLAUDE.md 清理」+ 原 T-14 | `4229fe1` |

| 番茄工作法砍掉（AI 无法计时、无法主动打断，违反硬约束 3）；西蒙学习法只留「目标拆分」，剔除「6 个月精通任意领域」这个营销化假承诺 | devlog「否决：番茄工作法进包」+「事实纠正：西蒙学习法」 | `32ddd12` |

**阶段收尾时的遗留**：任务跟踪从单文件 `backlog.md` 拆成 `plans/` 逐任务文档 + 本文件，见 `context.md` 活跃任务表。

---

## 阶段二 · MVP 实现（进行中）


| 结论 | 决策记录 | 落地提交 |
|---|---|---|
| 两个产物各有唯一权威副本：`skills/` 与 `output-styles/`，都在 git 内 | `plans/archive/2026-08-28-artifact-version-control.md` | `acfbf58` `cccdea7` |
| `.claude/skills/` 副本删除——使用方式是插件安装，本地加载路径没有存在理由，junction 同步方案随前提一并作废 | 同上 | `cccdea7` |
| owncc 规则文本改英文，约束原意与输出语言（中文）均不变 | 无独立记录，见 commit | `6e85cea` |
