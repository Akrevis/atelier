# 缺陷与待办跟踪

> 每次开发从这里挑，按优先级自上而下。解决一条就更新状态并记下**实际怎么解的**（可能和建议解法不同）。
> 已拍板的设计在 `design.md`，为什么这么定在 `devlog.md`。

**优先级**：`P0` 阻塞后续设计，不解决没法动手 ｜ `P1` 动手前必须定 ｜ `P2` 可边做边定 ｜ `P3` 远期/需先验证

**状态**：🔴 未解决 ｜ 🟡 进行中 ｜ 🟢 已解决 ｜ ⚪ 已搁置

---

## 总览

| 编号 | 标题 | 优先级 | 状态 |
|---|---|---|---|
| T-01 | MVP 划线（决定其他条目的优先级） | P0 | 🟢 |
| D-01 | 宽松验收与严格验收直接打架 | P0 | 🔴 |
| D-02 | palette 的「会话清空」与「分全局/项目」互斥 | P0 | 🔴 |
| D-03 | 中等复杂度（9~15 节点）的图掉进缝里 | P1 | 🔴 |
| D-04 | 复述验收的触发时机没定死 | P1 | 🔴 |
| D-05 | explain 与 code-walkthrough 边界重叠 | P1 | 🔴 |
| T-02 | 视觉标记表未定 | P1 | 🔴 |
| D-06 | 跨 agent 薄壳可能导致规则没进上下文 | P2 | 🟢 |
| D-07 | ASCII 中英混排对齐风险（样本只有 1） | P2 | 🟡 |
| T-03 | 沉浸模式激活入口：hook vs 命令 | P3 | 🔴 |
| T-04 | 番茄/西蒙的最终处置 | P2 | 🔴 |
| T-11 | 打包三件套（plugin.json / marketplace.json / README） | P2 | 🔴 |
| T-05 | 沉浸模式四阶段具体设计 | P3 | 🔴 |
| T-06 | SM-2 复习调度的数据结构 | P3 | 🔴 |
| T-07 | 错题本格式 | P3 | 🔴 |
| T-08 | 学习状态文件格式 | P3 | 🔴 |
| T-09 | 康奈尔/SQ3R 的落地形态 | P3 | 🔴 |
| T-10 | 通知钩子接口（Hermes/VPS 可选组件） | P3 | 🔴 |

---

## P0

### D-01 · 宽松验收与严格验收直接打架 🔴

**问题**：两条规则会同时在场，且互相矛盾——

- `explain`：核心对就行、不钻牛角尖、**不许因为用词不精确就打回**
- 沉浸模式验收：**不接受「大方向对」和解，错就是错**

**影响**：沉浸模式下学一个概念时，AI 走哪条无法确定，实际行为会随机。

**建议解法（待确认）**：**按「场景」切，不按「模式」切**——宽松是默认；严格只在用户主动喊「考我」时启用。沉浸模式下的日常讲解也走宽松。

---

### D-02 · palette 的「会话清空」与「分全局/项目」互斥 🔴

**问题**：两条已定规则逻辑上冲突——

- 按会话清空 → palette 是**临时**的
- 分全局/项目存 → palette 是**持久且有归属**的

既然每次会话都清空，分全局/项目就没意义，反正活不过这次会话。

**建议解法（待确认）**：palette 降级为**当次会话的临时画板**，生成在当前工作目录，不分全局/项目。持久化知识地图是另一个需求，现在不做。

---

## P1

### D-03 · 中等复杂度的图掉进缝里 🔴

**问题**：复杂度路由有个断层——

| 节点数 | 现有规则 | 结果 |
|---|---|---|
| ≤8 | ASCII | ✅ |
| **9~15** | ASCII 画不下；`/palette` 又必须用户主动敲 | ❌ **AI 只能说「建议敲 /palette」然后用文字讲**——「能画就画」落空 |
| 更复杂 | 用户敲 `/palette` | ✅ |

**建议解法（待确认）**：中间地带**拆图或画主干**——15 节点拆成两张 8 节点的分层图，或只画主干省略细节节点。符合分层交付的精神，但规则里没写。

---

### D-04 · 复述验收的触发时机没定死 🔴

**问题**：现有表述是「用户没在追问、或只说懂了 → AI **可以**要一轮复述」。「可以」不是可执行的条件。

**影响**：每次讲完都要 → 烦；从不要 → 形同虚设。不定死则实际行为随机。

**建议解法**：无。**需要一条明确的触发条件**，待设计。

---

### D-05 · explain 与 code-walkthrough 边界重叠 🔴

**问题**：真实场景会同时命中两个 skill——

- 「帮我实现一个 LRU 缓存」→ 要写代码（code-walkthrough），但 LRU 本身要讲（explain）
- 「这段代码怎么工作的」→ 不写新代码，但也不是纯概念

**建议解法（待确认）**：**有代码产出 → code-walkthrough**（内部沿用 explain 的讲解规范）；**无代码产出 → explain**。

---

### T-02 · 视觉标记表未定 🔴

**问题**：已定「用功能性视觉标记（不是卖萌）」，但具体标记表没定。

**已知边界**：终端唯一天然带色的是**代码块语法高亮**；标记必须有功能（标示内容类型），不能表达情绪。

**建议解法**：定一张小表（如 ⚠️ 坑 / 💡 关键洞察 / ❌ 反模式），数量控制在 5 个以内——多了就成噪音。

---

## P2

### D-07 · ASCII 中英混排对齐风险 🔴

**问题**：只验过一次（TCP 那两张图，用户判「可以」），样本量 1。

**真实风险**：中文字符宽度是英文两倍，不同终端/字体处理不一致，框线容易崩。TCP 那张图中文少，不代表通过。

**建议解法**：需**实测**——出一张中英混排密集的图（中文标签 + 英文变量名混在框线里）验对齐。

---

### T-04 · 番茄/西蒙的最终处置 🔴

**建议解法（问过两轮，未确认）**：番茄砍掉（AI 无法计时、无法主动打断，违反硬约束 3）；西蒙只留「目标拆分」，剔除「6 个月精通任意领域」这个假承诺。

---

### T-11 · 打包三件套 🔴

`.claude-plugin/plugin.json` + `marketplace.json` + 双语 README。已定要**发布**，参照 groundwork 的结构。

**依赖**：T-01（MVP 定了才知道 README 写什么）。

---

## P3

以下条目全部**依赖 T-01**——若 MVP 不含沉浸模式，均留到第二版。

| 编号 | 内容 |
|---|---|
| T-03 | 沉浸模式激活入口。**已找到候选解法**：skill frontmatter 的 `paths` 字段可限定 skill 只在匹配路径下自动激活，可能免掉 hook。语义是「正在处理的文件匹配」而非「当前工作目录」，需实测确认 |
| T-05 | 沉浸模式四阶段（立项/执行/验收/复盘）的具体流程设计 |
| T-06 | SM-2 复习调度的数据结构（每个知识点的间隔、系数、下次到期） |
| T-07 | 错题本格式 |
| T-08 | 学习状态文件格式（进度、实际耗时、解锁记录） |
| T-09 | 康奈尔笔记模板 / SQ3R 的「先提问再学 + 主动回忆」怎么落地 |
| T-10 | 通用通知钩子接口（执行用户配的命令/webhook，不写死任何具体服务）；**核心功能必须在没有推送时也完整** |

---

## 已解决

### T-01 · MVP 划线 🟢

**结论**：第一版 = 日常模式两个 skill（`explain` / `code-walkthrough`）+ `/palette`。沉浸模式整块留第二版。

**连带影响**：T-03、T-05~T-10 全部降 P3，第一版不碰。

---

### D-06 · references/ 里的规则会不会没进上下文 🟢

**查证结果**（Claude Code 官方文档 `code.claude.com/docs/en/skills`）——三层加载时机是明确的：

| 内容 | 加载时机 |
|---|---|
| frontmatter 的 `description` | **常驻上下文**，AI 靠它决定要不要触发 |
| `SKILL.md` 正文 | skill 被触发时加载 |
| `references/` 下的文件 | **需要 AI 主动读取**，不自动进上下文 |

原文：「letting Claude access detailed reference material **only when needed**」「Large reference docs... **don't need to load into context every time**」。

**结论：担心是真的。`references/` 只能放「按需查阅」的材料，不能放「每次都要遵守」的规则。**

| 内容类型 | 放哪 |
|---|---|
| 每次都要遵守的规则（图在前不在后、注解是标签级、复述判据） | **必须写进 `SKILL.md` 正文** |
| 按需查阅的材料（ASCII 图样例库、题型模板库） | `references/` |

**修正原设计**：原本打算把「画板规范 + 视觉标记体系」放 `references/` 共享——**这是错的**，它们是每次都要遵守的规则。

**改法**：把这两块**压缩到最小可执行形态**（几行字），直接内联进两个 SKILL.md。重复几行的代价，远小于「规则根本没进上下文」的风险。`references/` 留给真正的查阅材料。

**官方还给了配套要求**：引用文件时必须在 SKILL.md 里说明**每个文件装什么、什么时候读**，否则 AI 不知道该不该读。另有硬指标：`SKILL.md` 控制在 500 行以内。

**顺带捞到的两个可用发现**：

1. **`paths` frontmatter 字段**——glob 模式限定 skill 只在匹配路径下自动激活。这是 T-03（沉浸模式局部作用）的候选解法，可能免掉 hook
2. **`disable-model-invocation: true`**——description 不进上下文，只有用户主动敲才加载。**正好是 `/palette` 要的**：已定「AI 只能建议、不能自动生成」，这个字段从机制上保证了它
